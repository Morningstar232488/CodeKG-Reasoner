{
    "count": 4,
    "data": [
        {
            "_id": "pandas.core.groupby.generic._maybe_mangle_lambdas",
            "name": "_maybe_mangle_lambdas",
            "module": "pandas.core.groupby.generic",
            "doc": "Make new lambdas with unique names.\n\nParameters\n----------\nagg_spec : Any\n    An argument to NDFrameGroupBy.agg.\n    Non-dict-like `agg_spec` are pass through as is.\n    For dict-like `agg_spec` a new spec is returned\n    with name-mangled lambdas.\n\nReturns\n-------\nmangled : Any\n    Same type as the input.\n\nExamples\n--------\n>>> _maybe_mangle_lambdas('sum')\n'sum'\n\n>>> _maybe_mangle_lambdas([lambda: 1, lambda: 2])  # doctest: +SKIP\n[<function __main__.<lambda_0>,\n <function pandas...._make_lambda.<locals>.f(*args, **kwargs)>]",
            "is_deprecated": false,
            "source_code": "def _maybe_mangle_lambdas(agg_spec: Any) -> Any:\n    \"\"\"\n    Make new lambdas with unique names.\n\n    Parameters\n    ----------\n    agg_spec : Any\n        An argument to NDFrameGroupBy.agg.\n        Non-dict-like `agg_spec` are pass through as is.\n        For dict-like `agg_spec` a new spec is returned\n        with name-mangled lambdas.\n\n    Returns\n    -------\n    mangled : Any\n        Same type as the input.\n\n    Examples\n    --------\n    >>> _maybe_mangle_lambdas('sum')\n    'sum'\n\n    >>> _maybe_mangle_lambdas([lambda: 1, lambda: 2])  # doctest: +SKIP\n    [<function __main__.<lambda_0>,\n     <function pandas...._make_lambda.<locals>.f(*args, **kwargs)>]\n    \"\"\"\n    is_dict = is_dict_like(agg_spec)\n    if not (is_dict or is_list_like(agg_spec)):\n        return agg_spec\n    mangled_aggspec = type(agg_spec)()  # dict or OrderdDict\n\n    if is_dict:\n        for key, aggfuncs in agg_spec.items():\n            if is_list_like(aggfuncs) and not is_dict_like(aggfuncs):\n                mangled_aggfuncs = _managle_lambda_list(aggfuncs)\n            else:\n                mangled_aggfuncs = aggfuncs\n\n            mangled_aggspec[key] = mangled_aggfuncs\n    else:\n        mangled_aggspec = _managle_lambda_list(agg_spec)\n\n    return mangled_aggspec\n",
            "signature": "(agg_spec:Any) -> Any",
            "parameters": {
                "agg_spec": {
                    "name": "agg_spec",
                    "description": "An argument to NDFrameGroupBy.agg.\nNon-dict-like `agg_spec` are pass through as is.\nFor dict-like `agg_spec` a new spec is returned\nwith name-mangled lambdas.",
                    "is_optional": false,
                    "default": null,
                    "annotation": "typing.Any",
                    "kind": "POSITIONAL_OR_KEYWORD"
                }
            },
            "returns_doc": {
                "description": "Same type as the input.",
                "type": "Any"
            },
            "raise_doc": null,
            "type": "function",
            "class": null,
            "pattern": "method_removal"
        },
        {
            "_id": "pandas.core.dtypes.inference._iterable_not_string",
            "name": "_iterable_not_string",
            "module": "pandas.core.dtypes.inference",
            "doc": "Check if the object is an iterable but not a string.\n\nParameters\n----------\nobj : The object to check.\n\nReturns\n-------\nis_iter_not_string : bool\n    Whether `obj` is a non-string iterable.\n\nExamples\n--------\n>>> _iterable_not_string([1, 2, 3])\nTrue\n>>> _iterable_not_string(\"foo\")\nFalse\n>>> _iterable_not_string(1)\nFalse",
            "is_deprecated": false,
            "source_code": "def is_string_like(obj):\n    \"\"\"\n    Check if the object is a string.\n\n    Parameters\n    ----------\n    obj : The object to check\n\n    Examples\n    --------\n    >>> is_string_like(\"foo\")\n    True\n    >>> is_string_like(1)\n    False\n\n    Returns\n    -------\n    is_str_like : bool\n        Whether `obj` is a string or not.\n    \"\"\"\n\n    return isinstance(obj, str)\n",
            "signature": "(obj)",
            "parameters": {
                "obj": {
                    "name": "obj",
                    "description": null,
                    "is_optional": false,
                    "default": null,
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                }
            },
            "returns_doc": {
                "description": "Whether `obj` is a non-string iterable.",
                "type": "bool"
            },
            "raise_doc": null,
            "type": "function",
            "class": null,
            "pattern": "method_removal"
        },
        {
            "_id": "pandas.core.apply.maybe_mangle_lambdas",
            "name": "maybe_mangle_lambdas",
            "module": "pandas.core.apply",
            "doc": "Make new lambdas with unique names.\n\nParameters\n----------\nagg_spec : Any\n    An argument to GroupBy.agg.\n    Non-dict-like `agg_spec` are pass through as is.\n    For dict-like `agg_spec` a new spec is returned\n    with name-mangled lambdas.\n\nReturns\n-------\nmangled : Any\n    Same type as the input.\n\nExamples\n--------\n>>> maybe_mangle_lambdas('sum')\n'sum'\n>>> maybe_mangle_lambdas([lambda: 1, lambda: 2])  # doctest: +SKIP\n[<function __main__.<lambda_0>,\n <function pandas...._make_lambda.<locals>.f(*args, **kwargs)>]",
            "is_deprecated": false,
            "source_code": "def maybe_mangle_lambdas(agg_spec: Any) -> Any:\n    \"\"\"\n    Make new lambdas with unique names.\n\n    Parameters\n    ----------\n    agg_spec : Any\n        An argument to GroupBy.agg.\n        Non-dict-like `agg_spec` are pass through as is.\n        For dict-like `agg_spec` a new spec is returned\n        with name-mangled lambdas.\n\n    Returns\n    -------\n    mangled : Any\n        Same type as the input.\n\n    Examples\n    --------\n    >>> maybe_mangle_lambdas('sum')\n    'sum'\n    >>> maybe_mangle_lambdas([lambda: 1, lambda: 2])  # doctest: +SKIP\n    [<function __main__.<lambda_0>,\n     <function pandas...._make_lambda.<locals>.f(*args, **kwargs)>]\n    \"\"\"\n    is_dict = is_dict_like(agg_spec)\n    if not (is_dict or is_list_like(agg_spec)):\n        return agg_spec\n    mangled_aggspec = type(agg_spec)()  # dict or OrderedDict\n\n    if is_dict:\n        for key, aggfuncs in agg_spec.items():\n            if is_list_like(aggfuncs) and not is_dict_like(aggfuncs):\n                mangled_aggfuncs = _managle_lambda_list(aggfuncs)\n            else:\n                mangled_aggfuncs = aggfuncs\n\n            mangled_aggspec[key] = mangled_aggfuncs\n    else:\n        mangled_aggspec = _managle_lambda_list(agg_spec)\n\n    return mangled_aggspec\n",
            "signature": "(agg_spec: 'Any') -> 'Any'",
            "parameters": {
                "agg_spec": {
                    "name": "agg_spec",
                    "description": "An argument to GroupBy.agg.\nNon-dict-like `agg_spec` are pass through as is.\nFor dict-like `agg_spec` a new spec is returned\nwith name-mangled lambdas.",
                    "is_optional": false,
                    "default": null,
                    "annotation": "Any",
                    "kind": "POSITIONAL_OR_KEYWORD"
                }
            },
            "returns_doc": {
                "description": "Same type as the input.",
                "type": "Any"
            },
            "raise_doc": null,
            "type": "function",
            "class": null,
            "pattern": "method_addition"
        },
        {
            "_id": "pandas.core.indexes.base.ensure_index",
            "name": "ensure_index",
            "module": "pandas.core.indexes.base",
            "doc": "Ensure that we have an index from some index-like object.\n\nParameters\n----------\nindex_like : sequence\n    An Index or other sequence\ncopy : bool, default False\n\nReturns\n-------\nindex : Index or MultiIndex\n\nSee Also\n--------\nensure_index_from_sequences\n\nExamples\n--------\n>>> ensure_index(['a', 'b'])\nIndex(['a', 'b'], dtype='object')\n\n>>> ensure_index([('a', 'a'),  ('b', 'c')])\nIndex([('a', 'a'), ('b', 'c')], dtype='object')\n\n>>> ensure_index([['a', 'a'], ['b', 'c']])\nMultiIndex([('a', 'b'),\n        ('a', 'c')],\n       )",
            "is_deprecated": false,
            "source_code": "def ensure_index(index_like: AnyArrayLike | Sequence, copy: bool = False) -> Index:\n    \"\"\"\n    Ensure that we have an index from some index-like object.\n\n    Parameters\n    ----------\n    index_like : sequence\n        An Index or other sequence\n    copy : bool, default False\n\n    Returns\n    -------\n    index : Index or MultiIndex\n\n    See Also\n    --------\n    ensure_index_from_sequences\n\n    Examples\n    --------\n    >>> ensure_index(['a', 'b'])\n    Index(['a', 'b'], dtype='object')\n\n    >>> ensure_index([('a', 'a'),  ('b', 'c')])\n    Index([('a', 'a'), ('b', 'c')], dtype='object')\n\n    >>> ensure_index([['a', 'a'], ['b', 'c']])\n    MultiIndex([('a', 'b'),\n            ('a', 'c')],\n           )\n    \"\"\"\n    if isinstance(index_like, Index):\n        if copy:\n            index_like = index_like.copy()\n        return index_like\n\n    if isinstance(index_like, ABCSeries):\n        name = index_like.name\n        return Index._with_infer(index_like, name=name, copy=copy)\n\n    if is_iterator(index_like):\n        index_like = list(index_like)\n\n    if isinstance(index_like, list):\n        if type(index_like) is not list:\n            # must check for exactly list here because of strict type\n            # check in clean_index_list\n            index_like = list(index_like)\n\n        if len(index_like) and lib.is_all_arraylike(index_like):\n            from pandas.core.indexes.multi import MultiIndex\n\n            return MultiIndex.from_arrays(index_like)\n        else:\n            return Index._with_infer(index_like, copy=copy, tupleize_cols=False)\n    else:\n        return Index._with_infer(index_like, copy=copy)\n",
            "signature": "(index_like: 'AnyArrayLike | Sequence', copy: 'bool' = False) -> 'Index'",
            "parameters": {
                "index_like": {
                    "name": "index_like",
                    "description": "An Index or other sequence",
                    "is_optional": false,
                    "default": null,
                    "annotation": "AnyArrayLike | Sequence",
                    "kind": "POSITIONAL_OR_KEYWORD"
                },
                "copy": {
                    "name": "copy",
                    "description": null,
                    "is_optional": true,
                    "default": "False",
                    "annotation": "bool",
                    "kind": "POSITIONAL_OR_KEYWORD"
                }
            },
            "returns_doc": {
                "description": null,
                "type": "Index or MultiIndex"
            },
            "raise_doc": null,
            "type": "function",
            "class": null,
            "pattern": "method_addition"
        }
    ]
}