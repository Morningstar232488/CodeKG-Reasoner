{
    "data": {
        "index_table_from_tensor": {
            "_id": "tensorflow.contrib.lookup.lookup_ops.index_table_from_tensor",
            "name": "index_table_from_tensor",
            "module": "tensorflow.contrib.lookup.lookup_ops",
            "doc": "Returns a lookup table that converts a string tensor into int64 IDs.\n\nThis operation constructs a lookup table to convert tensor of strings into\nint64 IDs. The mapping can be initialized from a string `mapping` 1-D tensor\nwhere each element is a key and corresponding index within the tensor is the\nvalue.\n\nAny lookup of an out-of-vocabulary token will return a bucket ID based on its\nhash if `num_oov_buckets` is greater than zero. Otherwise it is assigned the\n`default_value`.\nThe bucket ID range is `[mapping size, mapping size + num_oov_buckets - 1]`.\n\nThe underlying table must be initialized by calling\n`tf.tables_initializer.run()` or `table.init.run()` once.\n\nElements in `mapping` cannot have duplicates, otherwise when executing the\ntable initializer op, it will throw a `FailedPreconditionError`.\n\nSample Usages:\n\n```python\nmapping_strings = tf.constant([\"emerson\", \"lake\", \"palmer\"])\ntable = tf.contrib.lookup.index_table_from_tensor(\n    mapping=mapping_strings, num_oov_buckets=1, default_value=-1)\nfeatures = tf.constant([\"emerson\", \"lake\", \"and\", \"palmer\"])\nids = table.lookup(features)\n...\ntf.tables_initializer().run()\n\nids.eval()  ==> [0, 1, 3, 2]\n```\n\nArgs:\n  mapping: A 1-D `Tensor` that specifies the mapping of keys to indices. The\n    type of this object must be castable to `dtype`.\n  num_oov_buckets: The number of out-of-vocabulary buckets.\n  default_value: The value to use for out-of-vocabulary feature values.\n    Defaults to -1.\n  hasher_spec: A `HasherSpec` to specify the hash function to use for\n    assignment of out-of-vocabulary buckets.\n  dtype: The type of values passed to `lookup`. Only string and integers are\n    supported.\n  name: A name for this op (optional).\n\nReturns:\n  The lookup table to map an input `Tensor` to index `int64` `Tensor`.\n\nRaises:\n  ValueError: If `mapping` is invalid.\n  ValueError: If `num_oov_buckets` is negative.",
            "is_deprecated": false,
            "source_code": "def index_table_from_tensor(mapping,\n                            num_oov_buckets=0,\n                            default_value=-1,\n                            hasher_spec=FastHashSpec,\n                            dtype=dtypes.string,\n                            name=None):\n  \"\"\"Returns a lookup table that converts a string tensor into int64 IDs.\n\n  This operation constructs a lookup table to convert tensor of strings into\n  int64 IDs. The mapping can be initialized from a string `mapping` 1-D tensor\n  where each element is a key and corresponding index within the tensor is the\n  value.\n\n  Any lookup of an out-of-vocabulary token will return a bucket ID based on its\n  hash if `num_oov_buckets` is greater than zero. Otherwise it is assigned the\n  `default_value`.\n  The bucket ID range is `[mapping size, mapping size + num_oov_buckets - 1]`.\n\n  The underlying table must be initialized by calling\n  `tf.tables_initializer.run()` or `table.init.run()` once.\n\n  Elements in `mapping` cannot have duplicates, otherwise when executing the\n  table initializer op, it will throw a `FailedPreconditionError`.\n\n  Sample Usages:\n\n  ```python\n  mapping_strings = tf.constant([\"emerson\", \"lake\", \"palmer\"])\n  table = tf.contrib.lookup.index_table_from_tensor(\n      mapping=mapping_strings, num_oov_buckets=1, default_value=-1)\n  features = tf.constant([\"emerson\", \"lake\", \"and\", \"palmer\"])\n  ids = table.lookup(features)\n  ...\n  tf.tables_initializer().run()\n\n  ids.eval()  ==> [0, 1, 3, 2]\n  ```\n\n  Args:\n    mapping: A 1-D `Tensor` that specifies the mapping of keys to indices. The\n      type of this object must be castable to `dtype`.\n    num_oov_buckets: The number of out-of-vocabulary buckets.\n    default_value: The value to use for out-of-vocabulary feature values.\n      Defaults to -1.\n    hasher_spec: A `HasherSpec` to specify the hash function to use for\n      assignment of out-of-vocabulary buckets.\n    dtype: The type of values passed to `lookup`. Only string and integers are\n      supported.\n    name: A name for this op (optional).\n\n  Returns:\n    The lookup table to map an input `Tensor` to index `int64` `Tensor`.\n\n  Raises:\n    ValueError: If `mapping` is invalid.\n    ValueError: If `num_oov_buckets` is negative.\n  \"\"\"\n  if mapping is None:\n    raise ValueError(\"mapping must be specified.\")\n  return lookup_ops.index_table_from_tensor(\n      vocabulary_list=mapping,\n      num_oov_buckets=num_oov_buckets,\n      default_value=default_value,\n      hasher_spec=hasher_spec,\n      dtype=dtype,\n      name=name)\n",
            "signature": "(mapping, num_oov_buckets=0, default_value=-1, hasher_spec=HasherSpec(hasher='fasthash', key=None), dtype=tf.string, name=None)",
            "parameters": {
                "mapping": {
                    "name": "mapping",
                    "description": "A 1-D `Tensor` that specifies the mapping of keys to indices. The\ntype of this object must be castable to `dtype`.",
                    "is_optional": false,
                    "default": null,
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                },
                "num_oov_buckets": {
                    "name": "num_oov_buckets",
                    "description": "The number of out-of-vocabulary buckets.",
                    "is_optional": true,
                    "default": "0",
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                },
                "default_value": {
                    "name": "default_value",
                    "description": "The value to use for out-of-vocabulary feature values.\nDefaults to -1.",
                    "is_optional": true,
                    "default": "-1",
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                },
                "hasher_spec": {
                    "name": "hasher_spec",
                    "description": "A `HasherSpec` to specify the hash function to use for\nassignment of out-of-vocabulary buckets.",
                    "is_optional": true,
                    "default": "HasherSpec(hasher='fasthash', key=None)",
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                },
                "dtype": {
                    "name": "dtype",
                    "description": "The type of values passed to `lookup`. Only string and integers are\nsupported.",
                    "is_optional": true,
                    "default": "<dtype: 'string'>",
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                },
                "name": {
                    "name": "name",
                    "description": "A name for this op (optional).",
                    "is_optional": true,
                    "default": "None",
                    "annotation": null,
                    "kind": "POSITIONAL_OR_KEYWORD"
                }
            },
            "returns_doc": {
                "description": "The lookup table to map an input `Tensor` to index `int64` `Tensor`.",
                "type": null
            },
            "raise_doc": [
                {
                    "type": "ValueError",
                    "description": "If `mapping` is invalid."
                },
                {
                    "type": "ValueError",
                    "description": "If `num_oov_buckets` is negative."
                }
            ],
            "type": "function",
            "class": null
        }
    }
}